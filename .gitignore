.env.local
.env.docker.local
db_backups/
.env.symfony.migrated*
# Generated by `task _dc_compile` — site-specific, never commit. Also keeps a
# later `git checkout` of the 3.x branch from colliding with the tracked file.
docker-compose.yml
